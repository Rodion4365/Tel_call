# CI/CD Настройка для Tel Call

Этот документ описывает настройку автоматического тестирования и развертывания проекта Tel Call.

## 📋 Содержание

- [Обзор](#обзор)
- [Настройка GitHub Actions](#настройка-github-actions)
- [Локальное развертывание](#локальное-развертывание)
- [Автоматическое развертывание](#автоматическое-развертывание)
- [Troubleshooting](#troubleshooting)

## 🎯 Обзор

CI/CD pipeline включает:

1. **Continuous Integration (CI)**:
   - Линтинг кода (backend + frontend)
   - Тесты backend с PostgreSQL
   - Проверка типов TypeScript
   - Сборка Docker образов

2. **Continuous Deployment (CD)**:
   - Автоматический деплой на production при push в `main`
   - Проверка работоспособности после деплоя
   - Уведомления о статусе

## 🔧 Настройка GitHub Actions

### 1. Добавьте секреты в GitHub

Перейдите в **Settings → Secrets and variables → Actions** и добавьте:

| Секрет | Описание | Пример |
|--------|----------|--------|
| `SSH_HOST` | IP адрес сервера | `45.67.89.123` |
| `SSH_USER` | Пользователь SSH | `root` |
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | `-----BEGIN RSA PRIVATE KEY-----...` |
| `SSH_PORT` | Порт SSH (опционально) | `22` |

### 2. Генерация SSH ключа

На вашей локальной машине:

```bash
# Генерация нового SSH ключа
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy

# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_deploy.pub root@YOUR_SERVER_IP

# Скопируйте приватный ключ для GitHub Secrets
cat ~/.ssh/github_deploy
```

Добавьте содержимое приватного ключа в секрет `SSH_PRIVATE_KEY`.

### 3. Настройка сервера

На сервере выполните:

```bash
# Убедитесь что проект установлен
cd /opt/app/projects/tel_call

# Сделайте скрипты исполняемыми
chmod +x scripts/*.sh

# Проверьте что deploy.sh работает
sudo ./scripts/deploy.sh main
```

## 🚀 Локальное развертывание

### Первая установка

```bash
# 1. Клонируйте репозиторий на сервер
git clone https://github.com/Rodion4365/Tel_call.git /opt/app/projects/tel_call
cd /opt/app/projects/tel_call

# 2. Запустите скрипт установки
sudo ./scripts/setup.sh

# 3. Отредактируйте конфигурационные файлы
nano backend/.env
nano infra/.env

# 4. Перезапустите контейнеры
cd infra
docker compose restart
```

### Ручное обновление

```bash
# Запустите скрипт деплоя
cd /opt/app/projects/tel_call
sudo ./scripts/deploy.sh main

# Или для другой ветки
sudo ./scripts/deploy.sh develop
```

## ⚡ Автоматическое развертывание

### Workflow срабатывает автоматически

1. **При Push в main**:
   ```bash
   git push origin main
   ```
   → Запускаются тесты → Docker build → Деплой на production

2. **При Pull Request**:
   → Только тесты и проверки (без деплоя)

### Мониторинг деплоя

1. Откройте **Actions** в GitHub репозитории
2. Выберите последний workflow run
3. Отслеживайте прогресс в реальном времени

### Отмена деплоя

Если что-то пошло не так:

```bash
# На сервере откатитесь к предыдущему коммиту
cd /opt/app/projects/tel_call
git checkout <previous_commit_hash>
sudo ./scripts/deploy.sh
```

## 🐛 Troubleshooting

### CI/CD не запускается

**Проблема**: Workflow не срабатывает при push

**Решение**:
```bash
# Проверьте что файл workflow существует
cat .github/workflows/ci-cd.yml

# Убедитесь что файл закоммичен
git add .github/workflows/ci-cd.yml
git commit -m "Add CI/CD workflow"
git push
```

### SSH подключение не работает

**Проблема**: `Permission denied (publickey)`

**Решение**:
```bash
# На сервере проверьте authorized_keys
cat ~/.ssh/authorized_keys

# Проверьте права
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Попробуйте подключиться вручную
ssh -i ~/.ssh/github_deploy root@YOUR_SERVER_IP
```

### Деплой завершается с ошибкой

**Проблема**: Деплой падает на этапе `docker compose up`

**Решение**:
```bash
# На сервере проверьте логи
cd /opt/app/projects/tel_call/infra
docker compose logs

# Проверьте .env файлы
ls -la ../backend/.env
ls -la .env

# Попробуйте запустить вручную
docker compose up -d --build
```

### Тесты не проходят

**Проблема**: Backend tests failed

**Решение**:
```bash
# Локально запустите тесты
cd backend
python -m pytest tests/ -v

# Проверьте что все зависимости установлены
pip install -r requirements.txt
```

## 📊 Метрики и мониторинг

### Проверка работоспособности

```bash
# Health check
curl https://api.callwith.ru/health

# Traefik dashboard
curl http://YOUR_SERVER_IP:8080/dashboard/

# Логи backend
cd /opt/app/projects/tel_call/infra
docker compose logs -f backend

# Логи всех сервисов
docker compose logs -f
```

### Rollback при проблемах

```bash
# 1. Найдите предыдущий рабочий коммит
git log --oneline -10

# 2. Откатитесь
git checkout <commit_hash>

# 3. Задеплойте
sudo ./scripts/deploy.sh

# 4. Если всё работает, сделайте hard reset
git reset --hard <commit_hash>
git push --force origin main
```

## 🔐 Безопасность

### Рекомендации

1. **Не храните секреты в коде**
   - Используйте GitHub Secrets
   - .env файлы в .gitignore

2. **Ограничьте SSH доступ**
   ```bash
   # На сервере создайте отдельного пользователя для деплоя
   useradd -m -s /bin/bash deployer
   usermod -aG docker deployer

   # Используйте его вместо root
   ```

3. **Ротация ключей**
   - Меняйте SSH ключи каждые 6 месяцев
   - Удаляйте старые ключи из authorized_keys

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Traefik Documentation](https://doc.traefik.io/traefik/)

---

**Вопросы?** Создайте issue в GitHub репозитории.
