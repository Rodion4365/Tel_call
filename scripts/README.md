# 🛠️ Deployment Scripts

Скрипты для автоматизации развертывания проекта Tel Call.

## 📁 Файлы

### `setup.sh`

Скрипт первой установки проекта на сервер.

**Использование:**
```bash
sudo ./scripts/setup.sh
```

**Что делает:**
- ✅ Устанавливает Docker и зависимости
- ✅ Создает структуру каталогов `/opt/app/projects/tel_call`
- ✅ Клонирует репозиторий
- ✅ Создает `.env` файлы из шаблонов
- ✅ Запускает контейнеры

**Требования:**
- Ubuntu 20.04+ / Debian 11+
- Root доступ
- Интернет соединение

**После установки:**
1. Отредактируйте `backend/.env` (BOT_TOKEN, SECRET_KEY)
2. Отредактируйте `infra/.env` (домены)
3. Перезапустите: `cd /opt/app/projects/tel_call/infra && docker compose restart`

---

### `deploy.sh`

Скрипт автоматического обновления/развертывания проекта.

**Использование:**
```bash
# Деплой main ветки
sudo ./scripts/deploy.sh main

# Деплой другой ветки
sudo ./scripts/deploy.sh develop
```

**Что делает:**
- ✅ Создает бэкап `.env` файлов
- ✅ Получает обновления из Git
- ✅ Проверяет конфигурацию
- ✅ Останавливает контейнеры
- ✅ Собирает новые образы
- ✅ Запускает контейнеры
- ✅ Проверяет работоспособность

**Требования:**
- Проект уже установлен через `setup.sh`
- Root доступ
- Git репозиторий настроен

**Безопасность:**
- Бэкапы сохраняются в `/opt/app/backups/`
- .env файлы не затрагиваются

---

## 🚀 Быстрый старт

### Первая установка

```bash
# 1. Скачайте репозиторий
git clone https://github.com/Rodion4365/Tel_call.git /tmp/tel_call
cd /tmp/tel_call

# 2. Запустите установку
sudo ./scripts/setup.sh

# 3. Настройте .env файлы
nano /opt/app/projects/tel_call/backend/.env
nano /opt/app/projects/tel_call/infra/.env

# 4. Перезапустите
cd /opt/app/projects/tel_call/infra
docker compose restart
```

### Обновление проекта

```bash
# Просто запустите деплой
cd /opt/app/projects/tel_call
sudo ./scripts/deploy.sh main
```

---

## 🔧 Интеграция с CI/CD

Эти скрипты используются в GitHub Actions для автоматического развертывания.

См. `.github/workflows/ci-cd.yml`:

```yaml
- name: Deploy to server
  uses: appleboy/ssh-action@v1.0.0
  with:
    host: ${{ secrets.SSH_HOST }}
    username: ${{ secrets.SSH_USER }}
    key: ${{ secrets.SSH_PRIVATE_KEY }}
    script: |
      cd /opt/app/projects/tel_call
      sudo ./scripts/deploy.sh main
```

---

## 🐛 Troubleshooting

### Permission denied

```bash
chmod +x scripts/*.sh
```

### Скрипт не находит проект

Убедитесь что запускаете из корня репозитория:
```bash
cd /opt/app/projects/tel_call
./scripts/deploy.sh main
```

### Бэкапы занимают много места

Удалите старые бэкапы:
```bash
find /opt/app/backups -name "env_backup_*.tar.gz" -mtime +30 -delete
```

---

## 📚 Дополнительная документация

- [Deployment Guide](../docs/DEPLOYMENT_GUIDE.md) - Полное руководство по развертыванию
- [CI/CD Setup](../docs/CICD_SETUP.md) - Настройка автоматического деплоя
- [Selectel Setup](../SELECTEL_SETUP.md) - Развертывание на Selectel
