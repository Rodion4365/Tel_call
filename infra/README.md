# Infrastructure

Docker Compose template for running the project locally. The compose file assumes Dockerfiles
exist in `../backend` and `../frontend`. Adjust image names, ports, and environment variables
as services evolve (TURN/STUN configuration, databases, etc.).
